#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main(){
	int a[10];
	int max;
	int i ;
	for (i=0;i<10;i++){ 
	scanf("%d",&a[i]);//不是a【10】 
	} 
	
   max=a[0];
   for (i=1;i<10;i++){
   	if (a[i]>max)
   	 max=a[i];
   }
   printf("%d",max);
   return 0;
}
