#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <string.h>
int main(){
	char str[2333];
	int lens1=0;
	int k=0;
	while((str[lens1++]=getchar())!='\n');
	for(int i=0;i<lens1-1;i++)
	{if(str[i]>'/'&&str[i]<':')
	k=k+1;
	}
printf("%d",k);
}
